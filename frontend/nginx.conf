server {
  listen 8080;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # SPA routing: serve index.html for unknown paths.
  location / {
    try_files $uri $uri/ /index.html;
  }
}

